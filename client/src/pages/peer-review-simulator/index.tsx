import React from 'react';
import PeerReviewSimulator from '@/components/review/PeerReviewSimulator';

const PeerReviewSimulatorPage: React.FC = () => {
  return <PeerReviewSimulator />;
};

export default PeerReviewSimulatorPage;